# Add inside the website's existing server { ... } block.
# Replace an existing location / block; do not create a duplicate.
# Keep the domain, document root and SSL settings managed by BaoTa.
location / {
    index index.html;
    try_files $uri $uri/ =404;
    error_page 404 /404.html;
}
