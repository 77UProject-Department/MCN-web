{
  "name": "qijiu-media-website",
  "version": "1.0.0",
  "private": true,
  "description": "柒玖视界直播传媒公司官网模板",
  "scripts": {
    "dev": "node scripts/serve.cjs",
    "build": "node scripts/generate-routes.cjs && npm run check",
    "check": "node --check dist/content.js && node --check dist/app.js && node scripts/check-site.cjs",
    "check:http": "npm run check -- --http"
  }
}
